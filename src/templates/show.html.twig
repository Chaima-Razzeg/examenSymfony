{% extends 'base.html.twig' %}
{% block title %}{{ product.nom }}{% endblock %}
{% block body %}
<h1>{{ product.nom }}</h1>
<p>{{ product.description }}</p>
<p>{{ product.prix }}</p>
<p>{{ product.image }}</p>

<hr>
<a href="/">Retour</a>
{% endblock %}